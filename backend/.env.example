NODE_ENV=development
PORT=3000
CORS_ORIGIN=http://localhost:5173

# Used when a real Gemini API key is provided; otherwise mock mode is used.
GEMINI_API_KEY=

# Reserved for the future cloud phase (auth + database).
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
JWT_SECRET=